spring:
  datasource:
    url: jdbc:mysql://mysql:3306/enterprise_iam?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${MYSQL_USER:iam_user}
    password: ${MYSQL_PASSWORD:iam_password}
  data:
    redis:
      host: redis
      port: 6379
      password: ${REDIS_PASSWORD:}
  mail:
    host: mailhog
    port: 1025
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}

app:
  jwt:
    secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  cors:
    allowed-origins: ${CORS_ORIGINS:http://localhost:3000}
